<Window x:Class="CardRPG.UI.CardRewardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Card Reward" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Black">

    <Grid>
        <Image Source="Assets/bg_combat.png" Stretch="Fill"
               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Rectangle Fill="#DD0A0A15"/>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="CHOOSE YOUR REWARD" FontSize="28" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,4">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FFD700" Offset="0"/>
                        <GradientStop Color="#FFA500" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
                <TextBlock.Effect>
                    <DropShadowEffect Color="#FFD700" BlurRadius="14" ShadowDepth="0" Opacity="0.4"/>
                </TextBlock.Effect>
            </TextBlock>

            <TextBlock Grid.Row="1" Text="Pick one card to add to your deck"
                       FontSize="13" Foreground="#8B8B7A" HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>

            <WrapPanel Grid.Row="2" HorizontalAlignment="Center"
                       VerticalAlignment="Center" x:Name="CardPanel"/>

            <Button Grid.Row="3" Content="Skip Reward" Width="160" Height="40"
                    Style="{StaticResource RpgSubtleButton}" FontSize="13"
                    Click="SkipButton_Click" HorizontalAlignment="Center"
                    Margin="0,10,0,0"/>
        </Grid>
    </Grid>
</Window>
