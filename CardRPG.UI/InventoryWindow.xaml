<Window x:Class="CardRPG.UI.InventoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventory"
        Background="Black"
        WindowState="Maximized"
        WindowStyle="None"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Grid>
        <Image Source="Assets/bg_city.avif" Stretch="Fill"
               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Rectangle Fill="#DD0A0A15"/>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="INVENTORY" FontSize="28" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,8">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#44AACC" Offset="0"/>
                        <GradientStop Color="#2288AA" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
                <TextBlock.Effect>
                    <DropShadowEffect Color="#44CCFF" BlurRadius="12" ShadowDepth="0" Opacity="0.3"/>
                </TextBlock.Effect>
            </TextBlock>

            <Border Grid.Row="1" Style="{StaticResource RpgPanel}" Padding="14,10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="EQUIPPED" Foreground="#888" FontSize="11" FontWeight="Bold" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Weapon: " Foreground="#8B8B7A" FontSize="14"/>
                            <TextBlock x:Name="WeaponTxt" Text="None" Foreground="LightBlue"
                                       FontSize="14" FontWeight="Bold">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="LightBlue" BlurRadius="6" ShadowDepth="0" Opacity="0.3"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>

                    <Rectangle Grid.Column="1" Width="1" Fill="#333" Margin="12,0"/>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="STATUS" Foreground="#888" FontSize="11" FontWeight="Bold" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="HP: " Foreground="#8B8B7A" FontSize="14"/>
                            <TextBlock x:Name="HpTxt" Text="" Foreground="LightGreen"
                                       FontSize="14" FontWeight="Bold"/>
                            <TextBlock Text="    Deck: " Foreground="#8B8B7A" FontSize="14" Margin="10,0,0,0"/>
                            <TextBlock x:Name="DeckCountTxt" Text="0 cards" Foreground="#FFCC44"
                                       FontSize="14" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource RpgPanel}" Padding="10">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="ITEMS" Foreground="Gold"
                                   FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
                        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                                    HorizontalAlignment="Center" Margin="0,8,0,0">
                            <Button Content="Use / Equip" Width="130" Height="42"
                                    Style="{StaticResource RpgTealButton}" FontSize="13"
                                    Click="UseButton_Click" Margin="0,0,8,0"/>
                            <Button Content="Drop" Width="90" Height="42"
                                    Style="{StaticResource RpgRedButton}" FontSize="13"
                                    Click="DropButton_Click"/>
                        </StackPanel>
                        <ListBox x:Name="ItemList" Background="Transparent"
                                 Foreground="White" FontSize="13" BorderThickness="0"
                                 SelectionChanged="ItemList_SelectionChanged"/>
                    </DockPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource RpgPanel}" Padding="10">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="CARD COLLECTION" Foreground="#FFCC44"
                                   FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6" HorizontalAlignment="Center">
                            <TextBlock Text="Sort: " Foreground="#888" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <Button Content="Type" Width="75" Height="38" FontSize="12"
                                    Style="{StaticResource RpgSubtleButton}"
                                    Click="SortByType_Click" Margin="0,0,4,0"/>
                            <Button Content="Rarity" Width="80" Height="38" FontSize="12"
                                    Style="{StaticResource RpgSubtleButton}"
                                    Click="SortByRarity_Click" Margin="0,0,4,0"/>
                            <Button Content="Cost" Width="75" Height="38" FontSize="12"
                                    Style="{StaticResource RpgSubtleButton}"
                                    Click="SortByCost_Click" Margin="0,0,12,0"/>
                            <Rectangle Width="1" Fill="#333" Margin="4,2"/>
                            <TextBlock Text="  Filter: " Foreground="#888" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <Button Content="All" Width="60" Height="38" FontSize="12"
                                    Style="{StaticResource RpgSubtleButton}"
                                    Click="FilterAll_Click" Margin="0,0,4,0"/>
                            <Button Content="ATK" Width="60" Height="38" FontSize="12"
                                    Style="{StaticResource RpgSubtleButton}"
                                    Click="FilterAttack_Click" Margin="0,0,4,0"/>
                            <Button Content="DEF" Width="60" Height="38" FontSize="12"
                                    Style="{StaticResource RpgSubtleButton}"
                                    Click="FilterDefense_Click" Margin="0,0,4,0"/>
                            <Button Content="PWR" Width="60" Height="38" FontSize="12"
                                    Style="{StaticResource RpgSubtleButton}"
                                    Click="FilterPower_Click"/>
                        </StackPanel>
                        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                                    HorizontalAlignment="Center" Margin="0,8,0,0">
                            <Button Content="Upgrade" Width="110" Height="40"
                                    Style="{StaticResource RpgGreenButton}" FontSize="13"
                                    Click="UpgradeCardButton_Click" Margin="0,0,8,0"/>
                            <Button Content="Sell Card" Width="110" Height="40"
                                    Style="{StaticResource RpgRedButton}" FontSize="13"
                                    Click="SellCardButton_Click" Margin="0,0,8,0"/>
                            <TextBlock x:Name="SellInfoTxt" Foreground="Gray" FontSize="11"
                                       VerticalAlignment="Center" Text="Click a card to select it"/>
                        </StackPanel>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <WrapPanel x:Name="CardPanel" Orientation="Horizontal"/>
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </Grid>

            <Border Grid.Row="3" Style="{StaticResource RpgPanel}"
                    Padding="10,6" Margin="0,10,0,0">
                <DockPanel>
                    <TextBlock x:Name="StatusTxt" Foreground="LimeGreen" FontSize="13"
                               VerticalAlignment="Center"/>
                    <Button DockPanel.Dock="Right" Content="Close" Width="100" Height="42"
                            Style="{StaticResource RpgSubtleButton}" FontSize="14"
                            Click="CloseButton_Click"/>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
